<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SimpleApp.Views.TimePg"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:vm="clr-namespace:SimpleApp.ViewModels" 
    x:DataType="vm:TimePgVM"
    BackgroundColor="WhiteSmoke"
    NavigationPage.HasNavigationBar="False">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" HorizontalOptions="Center"
                     Padding="10">
            <Label Text="Time Api Page" FontSize="20" FontAttributes="Bold"/>
            <Label Text="{Binding Url}"/>
            <Label Text="{Binding CurrentTime}"/>
            <Label Text="{Binding TimeZone}"/>
            <Label Text="{Binding UtcOffset}"/>
            <Label Text="{Binding JsonUntouched}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>